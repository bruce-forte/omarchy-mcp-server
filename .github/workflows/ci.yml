name: CI

# The suite reads a committed snapshot of `omarchy commands --json` rather than
# the installed system, so everything except the desktop tests runs on a plain
# runner with no Omarchy, no Hyprland, and no Wayland session. Those skip
# themselves.
on:
  push:
    branches: [master]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --frozen

      - name: Run tests
        run: uv run --frozen pytest -q

      - name: Lint
        # The same pinned ruff `make lint` runs. `ruff format` is deliberately
        # not run; see the note in pyproject.toml.
        run: uv run --frozen ruff check .

      - name: Type check
        # The same pinned pyright `make lint` runs, over src, tests and
        # examples. A type error fails the build.
        run: uv run --frozen pyright

      - name: TOOLS.md is up to date
        # It is generated from the server's own schemas, so a tool whose
        # description or arguments changed without regenerating it means the
        # documentation is already wrong.
        run: |
          PYTHONPATH=src uv run --frozen python -m tests.generate_tools_doc > /tmp/TOOLS.md
          diff -u TOOLS.md /tmp/TOOLS.md || {
            echo "::error::TOOLS.md is stale. Run 'make tools' and commit the result."
            exit 1
          }

      - name: The example config pins no defaults
        # Every key ships commented out. A live key would freeze that default
        # for everyone who installed before it changed.
        run: |
          grep -vE '^\s*(#|$)' config.example.toml | grep -vE '^\[' && {
            echo "::error::config.example.toml has an uncommented key."
            exit 1
          } || true

      - name: No agent-control files committed
        # `omarchy plugin add` clones this repository into
        # ~/.config/omarchy/plugins/<id>/, where a coding agent working in or
        # above that directory would read a CLAUDE.md, an AGENTS.md or a
        # .claude/ skill as instructions -- an instruction channel into somebody
        # else's agent, shipped by the one plugin that hands an agent command
        # execution. See ROADMAP N18. `make agents` is the same check a desktop
        # runs, so this cannot pass here and fail there.
        run: make agents

      - name: No virtualenv committed
        # `omarchy plugin validate` rejects symlinks inside a plugin folder, so
        # a committed .venv makes the plugin uninstallable. Asks git rather than
        # the filesystem: the step above creates a .venv here on purpose.
        run: |
          test -z "$(git ls-files .venv)" || {
            echo "::error::.venv is tracked by git. Remove it and keep it ignored."
            exit 1
          }

  shell:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v6
      - name: Parse the bootstrap wrapper
        run: bash -n bin/omarchy-mcpd
      - name: shellcheck
        # Through uv so the same command runs locally (`make lint`) and here,
        # rather than a CI-only action that cannot be reproduced on a desktop.
        run: uvx --from shellcheck-py shellcheck --severity=style bin/omarchy-mcpd
